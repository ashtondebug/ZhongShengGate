# 贡献指南

感谢你对《众生界》的关注与贡献。请遵守以下约定，让协作更顺畅。

## 报告问题

- 先搜索是否已有相同 issue，避免重复
- 标题简明扼要，描述清楚复现步骤、预期行为与实际行为
- 如有错误信息，请一并附上

## 提交代码（Pull Request）

1. Fork 本仓库，在独立分支上开发（命名建议：`feat/xxx`、`fix/xxx`）
2. 提交信息使用规范前缀：

   | 前缀 | 用途 |
   | --- | --- |
   | `feat:` | 新功能 |
   | `fix:` | 缺陷修复 |
   | `refactor:` | 重构，不改行为 |
   | `docs:` | 文档改动 |
   | `test:` | 测试相关 |
   | `style:` | 样式 / 格式 |

3. 保持改动聚焦：一个 PR 只做一件事
4. 提交前本地通过以下检查：

   ```bash
   npm run typecheck   # 类型检查
   npm test            # 单元测试
   npm run build       # 生产构建
   ```

5. 合并需要 ≥1 位维护者审查通过

## 代码规范

- 遵循项目架构分层：`pages`（UI）→ `hooks`（状态）→ `systems`（纯逻辑）→ `data`（数据）
- 游戏逻辑必须为纯函数、可单测，UI 层不写业务逻辑
- 新增逻辑必须补充对应单元测试（`tests/`）
- 文件命名小驼峰 / 短横线，类型命名大写开头
- 不引入不必要的新依赖

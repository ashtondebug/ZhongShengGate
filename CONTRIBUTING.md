# 贡献指南

感谢你对《众生界》（ZhongShengGate）的关注与贡献。本文档面向所有希望参与本项目的开发者，涵盖从报告问题、提交代码到代码合并的完整流程与规范。

---

## 目录

1. [项目概览](#项目概览)
2. [环境搭建](#环境搭建)
3. [如何报告问题](#如何报告问题)
4. [如何提交代码（Pull Request）](#如何提交代码pull-request)
5. [分支策略](#分支策略)
6. [提交信息规范](#提交信息规范)
7. [代码规范](#代码规范)
8. [测试要求](#测试要求)
9. [代码审查流程](#代码审查流程)
10. [行为准则](#行为准则)

---

## 项目概览

《众生界》是一款东方幻想题材的灵能战略 RPG 网页游戏。核心玩法循环为：**探索 → 获取资源 → 强化能力 → 解锁区域 → 挑战更强领域**。

技术栈：React 19 · TypeScript 5.8 · Vite 6 · Tailwind CSS 4 · Framer Motion · Vitest。

### 架构分层

```
pages       UI 层：只负责渲染与交互，不写业务逻辑
hooks       状态层：useGameState（reducer + localStorage 存档）
systems     逻辑层：纯函数，可单测（角色 / 战斗 / 探索 / 资源 / 物品 / 任务）
data        Mock 静态数据（路径 / 技能 / 敌人 / 区域 / 事件 / 物品 / 任务）
types       TypeScript 类型定义
```

> 新增功能时必须遵循上述分层，逻辑写入 `systems/` 纯函数，UI 只做消费。

---

## 环境搭建

### 环境要求

| 依赖 | 版本要求 |
| --- | --- |
| Node.js | ≥ 20（推荐 22 / 24） |
| npm | ≥ 10 |
| Git | ≥ 2.30 |

### 本地开发

```bash
# 克隆仓库
git clone https://github.com/ashtondebug/ZhongShengGate.git
cd ZhongShengGate

# 安装依赖
npm install

# 启动开发服务器（自动打开浏览器，端口 5173）
npm run dev
```

### 常用命令

| 命令 | 说明 |
| --- | --- |
| `npm run dev` | 启动开发服务器 |
| `npm run typecheck` | 类型检查 |
| `npm test` | 运行单元测试 |
| `npm run build` | 生产构建 |
| `npm run preview` | 本地预览生产构建 |

---

## 如何报告问题

### 提交前请确认

1. 搜索是否已有相同或相近的 issue，避免重复
2. 确认问题可稳定复现，而非一次性环境问题

### 问题描述模板

- **标题**：一句话概括问题（例如「探索页在手机端出现横向溢出」）
- **环境**：浏览器及版本、操作系统、屏幕尺寸（移动端 / 桌面端）
- **复现步骤**：1 / 2 / 3 步可复现的最小操作路径
- **预期行为**：应当发生什么
- **实际行为**：实际发生了什么（如有报错信息请附完整文本）
- **附加材料**：截图、控制台报错等

---

## 如何提交代码（Pull Request）

### 标准流程

1. **Fork 本仓库**，在自己的账号下得到副本
2. **克隆并新建分支**（分支命名见[分支策略](#分支策略)）
3. **提交改动**，提交信息遵循[提交信息规范](#提交信息规范)
4. **推送到自己的仓库**，然后创建 Pull Request 指向本仓库 `master`
5. 在 PR 中**填写模板**（已配置 `.github/PULL_REQUEST_TEMPLATE.md`）
6. 等待审查，根据反馈修改后重新推送

### 提交前检查清单

- [ ] `npm run typecheck` 通过
- [ ] `npm test` 通过
- [ ] `npm run build` 通过
- [ ] 新增逻辑已补充对应单元测试
- [ ] 未引入不必要的新依赖
- [ ] 代码遵循项目架构分层与风格

---

## 分支策略

### 分支命名

| 类型 | 前缀 | 示例 |
| --- | --- | --- |
| 新功能 | `feat/` | `feat/quest-system` |
| 缺陷修复 | `fix/` | `fix/battle-log-overflow` |
| 重构 | `refactor/` | `refactor/extract-battle` |
| 文档 | `docs/` | `docs/readme-update` |
| 测试 | `test/` | `test/quest-progress` |

### 分支原则

- **禁止**直接在 `master` 上开发与提交
- 一个分支 / 一个 PR 只做一件事，保持改动聚焦
- 合并前确保分支基于最新的 `master`（如有冲突先 rebase 或 merge 最新代码）

---

## 提交信息规范

使用 Conventional Commits 风格，格式为 `type: 简短描述`。

### 类型

| 类型 | 用途 |
| --- | --- |
| `feat:` | 新功能 |
| `fix:` | 缺陷修复 |
| `refactor:` | 重构，不改外部行为 |
| `docs:` | 文档改动 |
| `test:` | 测试相关 |
| `style:` | 样式 / 格式（不涉及逻辑） |
| `chore:` | 构建、工具链、依赖等杂项 |
| `perf:` | 性能优化 |

### 示例

```
feat: 增加背包出售功能
fix: 修复战斗日志移动端溢出
test: 补充任务进度推进的单元测试
```

> 描述用简洁中文，一般 50 字以内；如有必要，可在正文说明动机与影响范围。

---

## 代码规范

### 通用

- 文件命名：React 组件用大驼峰（`QuestCard.tsx`），工具文件用小驼峰 / 短横线（`questSystem.ts`）
- 类型与接口：大写开头（`QuestState`）；类型用 `interface`，联合类型用 `type`
- 代码注释：说明"为什么"而非"是什么"，避免废话注释
- 不引入不必要的新依赖；引入新依赖需在 PR 中说明理由

### 架构约束

- **业务逻辑必须写在 `systems/` 纯函数中**，禁止写在组件内
- 纯函数不允许访问 `localStorage`、`window` 等副作用；副作用集中在 `useGameState` reducer
- 数据定义放 `src/data/`，并导出类型；不硬编码魔法值
- 新页面需在 `App.tsx` 的 `renderScreen()` 与 `ScreenId` 类型中登记

### 样式约定

- 使用 Tailwind 工具类 + 玻璃拟态类（`.glass` / `.glass-strong` / `.glass-frost`）
- 移动端优先，桌面用 `sm:` / `md:` / `lg:` 渐进增强
- 颜色使用全局主题变量（灵晶青 / 灵力金 / 虚空紫），不引入随意色值

### 音效扩展

- 新增音效在 `src/audio/index.ts` 的 `SoundName` 联合类型与 `play()` switch 中注册
- 事件音由 `App.tsx` 的 `soundForEntry()` 按日志关键词映射

---

## 测试要求

### 基本原则

- 所有 `systems/` 纯函数逻辑必须有对应单元测试
- 测试文件位于 `tests/`，命名与 `systems/` 模块对应（如 `quests.test.ts`）
- 回归 bug 时，先补复现该 bug 的测试，再修代码

### 运行方式

```bash
npm test          # 运行全部测试
npx vitest -w     # 监听模式（开发时使用）
```

### 覆盖范围参考

- 角色：经验 / 升级 / 属性成长
- 战斗：伤害计算 / 防御减伤 / 奖励生成
- 探索：事件结算 / 属性判定
- 资源：支付 / 获得 / 支付能力判定
- 背包：物品入包 / 使用 / 出售
- 任务：接取 / 进度推进 / 奖励领取 / 重复领取保护
- 隐藏 BOSS：触发条件（等级 / 区域 / 概率）

---

## 代码审查流程

1. **自审**：提交前按[检查清单](#提交前检查清单)自查
2. **CI 校验**：PR 会自动触发 CI（类型检查 + 测试 + 构建），未通过前不可合并
3. **维护者审查**：所有代码需经 ≥1 位维护者（当前为仓库所有者）审查
4. **修改**：根据反馈更新代码并重新推送，保持对话解决所有评论
5. **合并**：全部通过后由维护者合并

---

## 行为准则

本项目遵循 GitHub 社区准则：

- 友善沟通，尊重每一位贡献者
- 建设性反馈，对事不对人
- 不接受任何形式的骚扰、歧视或人身攻击
- 拒绝恶意 / 破坏性提交（垃圾代码、后门、隐藏脚本等）

维护者有权拒绝违反上述准则的提交与评论。
